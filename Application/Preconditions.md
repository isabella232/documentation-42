# Preconditions for user's application 

1. The root directory of your application should contain all the required dockerfiles (link)
2. Name of dockerfiles and docker compose in your repo’s root (WIP)
